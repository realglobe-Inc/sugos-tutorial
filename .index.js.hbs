/**
 * {{pkg.description}}
 * @module {{pkg.name}}
 * @version {{pkg.version}}
 */
'use strict'

{{#each markdowns}}
exports.{{@key}} = [
{{#each contents}}  {
    title: '{{title}}',
    filename: '{{{filename}}}'
  }{{#if @last}}{{else}},
{{/if}}{{/each}}
]
{{/each}}